# 屏蔽资源 CRC 完整性校验逆向补丁

## 目标

修改 `/assets/Data/Resources/` 里的 lua/json 后，让进度条正常加载、不再弹「安装包损坏」。

## 背景

「安装包损坏」是结果，根因是资源 CRC 校验不匹配导致加载流程卡住。补丁思路是让校验不匹配时仍走「通过」路径。

## 定位方法（适用任意版本）

### 方法一（CRC 不匹配分支）

1. 搜索字符串 `crc mismatch %x != %x`。
2. 找它的交叉引用（`adrp` + `add`），得到错误分支的入口附近地址。
3. 从引用点向上回溯，找前面紧跟 `cmp` 的 `b.ne` / `b.eq` 条件跳转——即「CRC 不匹配就跳错误分支」处。
4. 把该条件跳转改为 `nop`。

### 方法二（反序列化校验返回值）

1. 搜索字符串 `Buffer does not contain a header`。
2. 找引用它的函数，该函数是资源 buffer 的反序列化校验函数。
3. 找调用该函数的 `bl`，其后紧跟的 `tbz w0, #0` 即「校验失败跳转」处。
4. 把该 `tbz` 改为 `nop`。

## 补丁（本版实例）

| 方法 | 文件偏移 | 原字节 | 新字节 | 汇编语义 |
| --- | --- | --- | --- | --- |
| 1 | `0x22BDB64` | `A1 07 00 54` | `1F 20 03 D5` | `b.ne` → `nop` |
| 2 | `0x22B9E34` | `A0 09 00 36` | `1F 20 03 D5` | `tbz w0,#0` → `nop` |

## 效果

CRC 或反序列化校验失败被当作通过，进度条走完、进入游戏、不弹窗。方法一为主，方法二为双保险。

## 提醒

改完需对 APK 重新做 v2/v3 签名。